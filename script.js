const canavsWidth = 600;
const canvasHeight = 600;

document.addEventListener("DOMContentLoaded", () => {

    const canvas = document.getElementById("canvas");
    canvas.width = canavsWidth;
    canvas.height = canvasHeight;
})